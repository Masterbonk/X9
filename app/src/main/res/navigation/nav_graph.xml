<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/fragment_main"
    android:id="@+id/nav_graph">

    <fragment
        android:id="@+id/fragment_main"
        android:name="dk.itu.moapd.x9.ADJU.MainFragment"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_main_to_report"
            app:destination="@id/fragment_create_report" />
    </fragment>

    <fragment
        android:id="@+id/fragment_create_report"
        android:name="dk.itu.moapd.x9.ADJU.CreateReportFragment"
        tools:layout="@layout/fragment_create_report">
        <action
            android:id="@+id/action_report_to_main"
            app:destination="@id/fragment_main" />
    </fragment>

</navigation>